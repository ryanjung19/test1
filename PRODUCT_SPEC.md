# AI 익명 매칭 앱 — 전체 기획 문서

**프로젝트명:** Spark (가칭)
**버전:** v1.0
**작성일:** 2026-03-18

---

## 목차

1. [서비스 개요](#1-서비스-개요)
2. [핵심 가치 제안](#2-핵심-가치-제안)
3. [타겟 사용자](#3-타겟-사용자)
4. [사용자 여정 전체 흐름](#4-사용자-여정-전체-흐름)
5. [기능 명세](#5-기능-명세)
6. [AI 에이전트 설계](#6-ai-에이전트-설계)
7. [익명성 및 안전 설계](#7-익명성-및-안전-설계)
8. [매칭 알고리즘](#8-매칭-알고리즘)
9. [채팅 샌드박스 설계](#9-채팅-샌드박스-설계)
10. [정보 교환 추천 시스템](#10-정보-교환-추천-시스템)
11. [기술 아키텍처](#11-기술-아키텍처)
12. [데이터 모델](#12-데이터-모델)
13. [화면 흐름 (UX Flow)](#13-화면-흐름-ux-flow)
14. [SMS 대화 스크립트 예시](#14-sms-대화-스크립트-예시)
15. [운영 정책](#15-운영-정책)
16. [개인정보 및 보안 정책](#16-개인정보-및-보안-정책)
17. [KPI 및 성공 지표](#17-kpi-및-성공-지표)
18. [개발 로드맵](#18-개발-로드맵)

---

## 1. 서비스 개요

### 한 줄 정의
> 전화번호 하나로 시작하는 AI 익명 매칭 서비스. 앱 설치 없이, 문자 한 통으로 새로운 사람을 만난다.

### 서비스 개념

Spark는 SMS를 유일한 인터페이스로 사용하는 AI 기반 익명 매칭 서비스다.
사용자는 특정 전화번호로 문자를 보내는 것만으로 서비스에 진입한다.

- **앱 설치 없음** — 스마트폰 기본 문자앱만 있으면 된다
- **회원가입 없음** — 전화번호가 유일한 식별자
- **익명 보장** — 상대방은 나의 실제 번호를 절대 알 수 없다
- **AI가 중간에서 모든 것을 처리** — 프로필 작성, 매칭, 대화 모니터링, 정보 교환 추천

### 핵심 플로우 요약

```
문자 한 통 전송
    → AI 에이전트 "Spark"와 대화 (6~10턴)
    → 익명 프로필 자동 생성
    → AI가 최적 상대 매칭
    → 가상번호 샌드박스에서 익명 채팅
    → AI가 대화 품질 판단
    → 상호 정보 교환 추천
```

---

## 2. 핵심 가치 제안

### 사용자 관점

| 기존 매칭앱의 문제 | Spark의 해결 방식 |
|---|---|
| 앱 설치 및 회원가입이 귀찮다 | 문자 한 통으로 즉시 시작 |
| 상대방이 나를 스토킹할 수 있다 | 실제 번호가 절대 노출되지 않음 |
| 프로필 사진/외모 중심 필터링 | 텍스트 대화로 내면 파악 |
| AI 없이 내가 직접 매칭 상대를 고른다 | AI가 진짜 잘 맞는 상대를 먼저 분석해서 연결 |
| 언제 연락처를 교환해야 할지 모른다 | AI가 적절한 타이밍에 추천 |

### 차별화 포인트

1. **Zero Friction** — 앱 설치, 회원가입, 사진 업로드 없음
2. **진짜 익명성** — Twilio 가상번호로 실제 번호 완전 보호
3. **AI 큐레이션** — 무작위 매칭이 아닌 AI 호환성 분석 기반 매칭
4. **중립적 관찰자** — AI가 채팅을 모니터링하여 관계 발전 타이밍 제안
5. **SMS 범용성** — 어떤 폰에서도 작동, 인터넷 불필요

---

## 3. 타겟 사용자

### Primary Target
- **연령대:** 20~35세
- **특성:** 새로운 사람을 만나고 싶지만 기존 앱의 피로감을 느끼는 사람
- **니즈:** 진지한 연애보다는 가벼운 친구 시작, 하지만 그게 연애로 발전할 수도

### Secondary Target
- 소셜 불안이 있어 첫 접촉이 어려운 사람 (AI가 아이스브레이커 역할)
- 해외 체류자나 새로운 도시 이주자 (새로운 인간관계 필요)
- 기술 친화적이지 않은 사용자 (앱 설치 없이 문자로만)

### 사용 목적 분류

| 타입 | 설명 | 비율(예상) |
|---|---|---|
| `friends` | 새로운 친구 사귀기 | 40% |
| `dating` | 연애 상대 찾기 | 35% |
| `both` | 일단 만나보고 결정 | 25% |

---

## 4. 사용자 여정 전체 흐름

### Phase 1: 진입 (Onboarding)

```
1. 사용자가 서비스 번호로 첫 문자 전송
        ↓
2. AI 에이전트 "Spark" 환영 인사
        ↓
3. 대화형 인터뷰 (6~10턴)
   - 관심사 / 취미
   - 성격 / 가치관
   - 원하는 관계 유형 (친구 / 연애 / 둘 다)
   - 나이대
   - 닉네임 (선택)
        ↓
4. AI가 프로필 자동 생성 및 요약 확인
        ↓
5. "매칭을 찾고 있어요" 알림 → waiting 상태
```

### Phase 2: 매칭 (Matching)

```
6. AI가 대기 중인 사용자 풀에서 호환성 분석
        ↓
7. 점수 0.65 이상인 상대 발견 시 → 즉시 매칭
        ↓
8. 두 사람 모두에게 매칭 알림 SMS 발송
   "새로운 매칭이 생겼어요! 상대방의 닉네임은 '별이'예요.
    공통 관심사: 독서, 카페 탐방
    문자를 보내서 대화를 시작해보세요!"
```

### Phase 3: 익명 채팅 (Anonymous Chat)

```
9. 사용자가 메시지 전송
        ↓
10. 서버가 실제 번호를 숨기고 가상번호로 상대에게 전달
        ↓
11. 상대방 응답도 동일하게 프록시 처리
        ↓
12. AI가 백그라운드에서 대화 품질 모니터링
    (매 10개 메시지마다 분석)
```

### Phase 4: 정보 교환 추천 (Info Exchange)

```
13. AI가 대화 품질 양호 판단 시
        ↓
14. 두 사람 모두에게 추천 SMS 발송
    "두 분의 대화가 정말 잘 이어지고 있어요! ✨
     서로 연락처를 교환할 준비가 됐다면
     '교환'이라고 답장해주세요."
        ↓
15. 둘 다 동의 시 → 실제 번호 공유 (또는 SNS 공유 선택)
    한 명만 동의 시 → 대화 계속, 재평가
```

### Phase 5: 이후 옵션

```
16-A. 연락처 교환 후 → 매칭 종료, 독립적으로 연락
16-B. 교환 거부 → 대화 계속, AI가 다음 타이밍에 재추천
16-C. LEAVE 명령 → 매칭 종료, waiting으로 복귀 (새 매칭 탐색)
```

---

## 5. 기능 명세

### 5.1 온보딩 시스템

**목적:** 사용자의 성격, 관심사, 원하는 관계를 대화를 통해 파악하여 익명 프로필 생성

**상세 기능:**
- AI가 자연스러운 대화 형식으로 인터뷰 진행
- 최소 6턴, 최대 12턴 이내 완료
- 추출 항목:
  - `display_name`: 닉네임 (선택, AI가 제안 가능)
  - `age_range`: 나이대 ("20대 초반", "30대 중반" 등)
  - `looking_for`: 목적 (친구 / 연애 / 둘 다)
  - `interests`: 관심사 배열 (최소 3개)
  - `personality`: 성격 특성 (최소 3개)
  - `bio_summary`: AI가 생성한 2~3줄 자연어 요약
- 완성된 프로필을 사용자에게 요약 문자로 전송하여 확인
- 수정 요청 가능 ("독서 말고 영화로 바꿔줘")

**예외 처리:**
- 응답이 너무 짧거나 모호할 경우 → 재질문
- 부적절한 내용 입력 시 → 정중하게 재안내
- 12턴 초과 시 → 현재까지 수집된 정보로 강제 완성

### 5.2 매칭 시스템

**목적:** 완성된 프로필을 기반으로 가장 잘 맞는 상대를 찾아 연결

**매칭 로직:**
1. `waiting` 상태 + 프로필 완성된 사용자 풀 탐색
2. 각 후보 대상 AI 호환성 점수 계산 (0.0~1.0)
3. `MATCH_THRESHOLD` (기본값 0.65) 이상 → 매칭 실행
4. 복수 후보 있을 경우 → 최고 점수 상대와 매칭
5. 후보 없을 경우 → waiting 유지, 새 사용자 온보딩 완료 시 재평가

**매칭 알림 메시지 구성:**
- 상대방 닉네임
- 공통 관심사 (최대 3개)
- AI가 매칭 이유 한 줄 설명
- 대화 시작 안내

**매칭 제약 조건:**
- `looking_for` 필드 호환성 우선 고려
  - `friends` ↔ `friends` (우선)
  - `dating` ↔ `dating` (우선)
  - `both`는 누구와도 가능
- 이미 매칭된 적 있는 상대와 재매칭 방지

### 5.3 익명 채팅 시스템

**목적:** 두 사람이 실제 번호를 모르는 상태에서 안전하게 대화

**동작 방식:**

```
사용자 A (실제: +8210-1111)  →  가상번호 A (+8210-9001)
                                        ↕ (서버 라우팅)
사용자 B (실제: +8210-2222)  ←  가상번호 B (+8210-9002)
```

- A가 보내는 문자의 발신번호: `+8210-9002` (B의 가상번호)
- B가 A에게 답하면 A에게는 `+8210-9001`에서 온 것처럼 표시
- 실제 번호는 서버 DB에만 저장

**메시지 전달 흐름:**
1. A가 가상번호 B로 문자 발송
2. Twilio webhook → 서버 수신
3. 서버: A의 실제 번호 → match 테이블 조회 → B의 실제 번호 확인
4. Twilio API: B의 실제 번호로 발송 (from: 가상번호 A)
5. B 수신: 가상번호 A로부터 온 메시지로 표시

**채팅 샌드박스 (웹):**
- SMS 외 웹 기반 채팅 UI 제공 (선택)
- 고유 URL: `https://app.spark.ai/chat/{channel_token}`
- SMS로 채팅 URL 공유 → 브라우저에서 실시간 채팅 가능
- WebSocket 기반 실시간 통신

### 5.4 AI 채팅 모니터링 시스템

**목적:** 두 사람의 대화를 AI가 분석하여 관계 발전 상태를 평가하고 최적의 정보 교환 타이밍 추천

**분석 시점:**
- 매 `MONITOR_INTERVAL` (기본값: 10)개 메시지마다 실행
- 한쪽이 대화를 오래 안 하면 (24시간 이상) 특별 체크

**분석 항목:**
- 대화 빈도 및 응답 속도
- 감정 톤 (긍정 / 중립 / 부정)
- 공통 주제 발견 빈도
- 상호 질문 횟수 (관심도 지표)
- 대화 지속 의지

**추천 조건 (모두 충족 시):**
- 총 메시지 수 20개 이상
- 두 사람 모두 응답 중 (한쪽만 일방적인 대화 아님)
- 감정 톤 긍정적
- AI 판단 점수 0.7 이상

### 5.5 특수 명령어 시스템

언제든지 아래 명령어를 문자로 보내면 즉시 처리:

| 명령어 | 동작 |
|---|---|
| `STOP` | 모든 서비스 중단, 번호 비활성화 |
| `START` | 서비스 재시작 |
| `HELP` | 도움말 전송 |
| `LEAVE` | 현재 매칭 종료, 새 매칭 탐색 |
| `교환` | 연락처 교환 동의 (AI 추천 후 사용 가능) |
| `프로필` | 내 현재 프로필 요약 조회 |
| `수정` | 프로필 수정 시작 |

---

## 6. AI 에이전트 설계

### 에이전트 이름 및 페르소나

**이름:** Spark
**성격:** 따뜻하고 호기심 많은 친구. 판단하지 않고 경청. 적당히 유머러스.
**말투:** 반말/존댓말 사용자에 맞춤 (첫 문자 말투 기준으로 맞춤)
**언어:** 한국어 기본, 영어 입력 시 영어로 응답

### 온보딩 에이전트 지침

```
[시스템 프롬프트 핵심]

당신은 'Spark'라는 AI 매칭 어시스턴트입니다.
목표: 6~10번의 자연스러운 대화를 통해 사용자의 프로필을 파악하세요.

[반드시 파악해야 할 정보]
1. 원하는 관계 유형 (친구 / 연애 / 둘 다)
2. 관심사 / 취미 (최소 3가지)
3. 성격 특성 (최소 3가지)
4. 나이대 (정확한 나이가 아닌 대략적인 범위)
5. 닉네임 (원하면, 없으면 AI가 제안)

[절대 물어보면 안 되는 것]
- 본명
- 거주 주소, 직장 주소
- 직장명, 학교명
- 실제 연락처

[대화 원칙]
- 심문이 아닌 대화처럼 자연스럽게
- 한 번에 하나의 질문만
- 사용자 답변에 반응 후 다음 질문
- 불편해 보이는 주제는 즉시 바꿔줌

[응답 형식]
반드시 아래 JSON으로 응답:
{
  "message": "사용자에게 보낼 텍스트",
  "extracted": { ... } or null,
  "profileComplete": false
}
```

### 호환성 평가 에이전트 지침

```
[시스템 프롬프트 핵심]

두 사람의 프로필을 비교하여 매칭 호환성을 평가하세요.

평가 기준 (가중치):
1. 관심사 겹침 (30%) — 공통 취미/관심사 수와 깊이
2. 가치관 유사성 (30%) — 성격 특성의 상호 보완 또는 유사성
3. 목적 일치도 (25%) — 친구/연애 목적 매칭
4. 나이대 근접성 (15%) — 나이대 차이

응답 형식:
{
  "score": 0.75,
  "reasoning": "두 분 모두 독서와 카페를 좋아하고...",
  "common_interests": ["독서", "카페"],
  "compatibility_highlights": ["비슷한 가치관", "같은 목적"]
}
```

### 채팅 모니터링 에이전트 지침

```
[시스템 프롬프트 핵심]

두 사람의 대화 기록을 분석하여 관계 발전 상태를 평가하세요.

분석 항목:
1. 상호 참여도 — 양방향 대화 여부
2. 감정 톤 — 전반적인 분위기
3. 관심도 지표 — 질문 횟수, 응답 깊이
4. 공통 주제 — 대화 중 자연스럽게 나온 공통점

추천 기준:
- 두 사람 모두 적극적으로 대화 참여 중
- 긍정적이고 따뜻한 분위기
- 서로에 대한 진짜 관심 보임

응답 형식:
{
  "recommend": true,
  "reason": "두 분이 서로에게 많은 질문을 하고...",
  "summary": "20개 메시지 동안 독서, 여행 등 공통 관심사를 발견...",
  "engagement_score": 0.82
}
```

---

## 7. 익명성 및 안전 설계

### 익명성 보장 체계

```
레벨 1: 전화번호 마스킹
  - 모든 SMS는 가상번호를 통해 중계
  - 상대방은 가상번호만 볼 수 있음
  - 실제 번호는 서버 DB에 암호화 저장

레벨 2: 프로필 익명성
  - 본명, 주소, 직장 등 개인 식별 정보 수집 금지
  - 닉네임은 선택사항
  - 나이는 "30대 초반" 등 범위만 사용

레벨 3: 대화 내용 보호
  - 채팅 내용은 AI 분석 목적으로만 사용
  - 90일 후 자동 삭제
  - 매칭 종료 후 양쪽 모두 동의 없이는 내용 공유 불가
```

### 안전 장치

| 위험 요소 | 대응 방법 |
|---|---|
| 부적절한 메시지 | AI 모니터링 + 키워드 필터링 → 경고 후 매칭 종료 |
| 스팸 전화번호 | Twilio lookup으로 번호 유효성 검증 |
| 개인정보 요구 | AI가 온보딩 중 절대 수집하지 않음 |
| 채팅 중 개인정보 공유 | AI 모니터링 감지 시 주의 문자 발송 |
| 악의적 사용자 | STOP 명령 또는 관리자 강제 비활성화 |

### 신고 기능

사용자가 `신고` 문자 전송 시:
1. 현재 매칭 즉시 중단
2. 최근 50개 메시지 관리자 검토 대기열 등록
3. 신고자에게 확인 문자 발송
4. 48시간 내 처리 완료

---

## 8. 매칭 알고리즘

### 알고리즘 개요

```
Phase 1: 후보 필터링 (DB 쿼리)
  - state = 'waiting'
  - profile.completed = 1
  - 이전 매칭 기록 없음
  - looking_for 호환 여부 (소프트 필터)

Phase 2: AI 호환성 점수 계산 (Claude API)
  - 각 후보 대상 Claude 호출
  - 0.0~1.0 점수 반환

Phase 3: 매칭 결정
  - 점수 >= MATCH_THRESHOLD (0.65) 인 후보 중 최고 점수 선택
  - 없으면 waiting 유지

Phase 4: 매칭 실행
  - matches 테이블 레코드 생성
  - 가상번호 할당
  - 두 사람 모두 상태 → matched
  - 매칭 알림 SMS 발송
```

### `looking_for` 매칭 매트릭스

| A \ B | friends | dating | both |
|---|---|---|---|
| friends | ✅ 최우선 | ❌ 비추천 | ✅ 가능 |
| dating | ❌ 비추천 | ✅ 최우선 | ✅ 가능 |
| both | ✅ 가능 | ✅ 가능 | ✅ 최우선 |

### 점수 계산 요소

```
최종 점수 = (관심사 점수 × 0.30)
          + (가치관 점수 × 0.30)
          + (목적 일치 점수 × 0.25)
          + (나이대 점수 × 0.15)
```

**나이대 점수 기준:**
- 같은 10년대 → 1.0
- 1단계 차이 → 0.7
- 2단계 차이 → 0.4
- 3단계 이상 차이 → 0.1

### 확장성 고려

소규모 (< 1,000명 동시 대기):
- 모든 후보에 대해 Claude API 직접 호출

대규모 확장 시:
1. 프로필 `bio_summary` 임베딩 생성
2. 코사인 유사도로 상위 10명 필터링
3. 상위 10명에 대해서만 Claude 점수 계산
4. `profiles.embedding` 컬럼 활용

---

## 9. 채팅 샌드박스 설계

### SMS 프록시 방식 (기본)

```
[가상번호 풀 관리]
- Twilio에서 가상번호 N개 구매
- 매칭 생성 시 풀에서 번호 2개 할당
  - proxy_number_a: 사용자 B가 A에게 연락할 때 보이는 번호
  - proxy_number_b: 사용자 A가 B에게 연락할 때 보이는 번호
- 매칭 종료 시 번호를 풀에 반환

[라우팅 로직]
인바운드 메시지:
  To = proxy_number_a → sender = B → recipient = A
  To = proxy_number_b → sender = A → recipient = B
```

### Twilio Proxy 방식 (옵션)

```
- Twilio Proxy Service 활용
- 세션 기반 관리 (Twilio가 번호 라우팅 자동 처리)
- 보안 강화, 관리 편의성 높음
- 비용: 세션당 $0.001 + 인터랙션당 $0.00 추가
```

### 웹 채팅 샌드박스 (선택 기능)

```
접속 방법:
  SMS로 채팅 링크 수신
  → https://spark.ai/chat/{channel_token}
  → 브라우저에서 실시간 채팅

기술 스택:
  - WebSocket (ws 라이브러리)
  - 미니멀 HTML/CSS/JS 채팅 UI
  - 토큰 기반 인증 (channel_token)

특징:
  - 모바일 최적화
  - SMS와 웹 채팅 병행 가능 (메시지 동기화)
  - 파일/이미지 전송 미지원 (v1)
```

---

## 10. 정보 교환 추천 시스템

### 추천 조건

다음 모든 조건 충족 시 추천 트리거:
1. 총 교환 메시지 수 ≥ 20개
2. 두 사람 모두 최근 24시간 내 메시지 보냄
3. AI 모니터링 점수 ≥ 0.70
4. 아직 추천을 보내지 않은 상태 (`recommendation_sent = 0`)

### 추천 메시지 예시

```
[두 사람에게 발송]

Spark: "두 분이 정말 잘 맞는 것 같아요! 🎉
       독서와 카페 탐방을 좋아하는 공통점도 있고,
       대화도 자연스럽게 이어지고 있어요.

       서로 연락처를 교환하고 싶다면
       지금 '교환'이라고 답장해주세요.
       상대방도 동의하면 서로의 번호를 알려드릴게요!"
```

### 동의 처리 로직

```
A가 '교환' → 서버: A 동의 기록
B가 '교환' → 서버: B 동의 기록

둘 다 동의 시:
  → A에게: "B의 실제 번호는 +8210-xxxx 입니다"
  → B에게: "A의 실제 번호는 +8210-xxxx 입니다"
  → 매칭 상태: info_exchange → closed
  → 채팅 채널 유지 (선택적으로 72시간 후 종료)

한 명만 동의 시:
  → 동의한 사람에게: "상대방이 아직 결정 중이에요. 대화를 계속해보세요!"
  → 동의 기록 유지, 상대방이 나중에 동의해도 처리

둘 다 거부 시:
  → "알겠어요! 대화를 계속 이어가세요. 나중에 다시 추천드릴게요."
  → 30개 메시지 후 재평가
```

---

## 11. 기술 아키텍처

### 시스템 구성도

```
┌─────────────────────────────────────────────────────────────────┐
│                        사용자 레이어                             │
│   📱 SMS App          🌐 Web Browser                            │
└─────────────────┬───────────────────┬───────────────────────────┘
                  │                   │
┌─────────────────▼───────────────────▼───────────────────────────┐
│                        Twilio 레이어                             │
│   SMS Inbound          SMS Outbound         Twilio Proxy        │
│   (Webhook)            (REST API)           (Optional)          │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                     애플리케이션 서버 (Express.js)               │
│                                                                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │
│  │   Routes     │  │  Middleware  │  │    WebSocket         │   │
│  │ /webhook/sms │  │  Twilio Sig  │  │   /ws/chat/:token    │   │
│  │ /chat/:token │  │  ErrorHandler│  │                      │   │
│  │ /health      │  │              │  │                      │   │
│  └──────┬───────┘  └──────────────┘  └──────────────────────┘   │
│         │                                                        │
│  ┌──────▼───────────────────────────────────────────────────┐   │
│  │                    Services 레이어                        │   │
│  │  StateMachine  →  Onboarding  →  Matching  →  Chat       │   │
│  │                                             →  Monitor   │   │
│  └──────┬──────────────────────────────────────────────────┘    │
│         │                                                        │
│  ┌──────▼──────────────┐  ┌───────────────────────────────────┐ │
│  │   Repositories      │  │        External Services          │ │
│  │  userRepo           │  │  Claude API (Anthropic)           │ │
│  │  profileRepo        │  │  Twilio SMS API                   │ │
│  │  matchRepo          │  │                                   │ │
│  │  messageRepo        │  └───────────────────────────────────┘ │
│  │  stateRepo          │                                        │
│  └──────┬──────────────┘                                        │
└─────────┼────────────────────────────────────────────────────────┘
          │
┌─────────▼──────────────────┐
│       SQLite DB             │
│  users / profiles / matches │
│  messages / conv_state      │
└─────────────────────────────┘
```

### 기술 스택

| 레이어 | 기술 | 이유 |
|---|---|---|
| 런타임 | Node.js 22 + TypeScript 5 | 타입 안전성, 풍부한 생태계 |
| 웹 프레임워크 | Express.js 4 | 단순, 경량, Twilio 친화적 |
| AI | Anthropic Claude API | 최신 한국어 이해 능력 |
| SMS | Twilio | 가상번호 프록시 기능 내장 |
| DB | SQLite (better-sqlite3) | 운영 단순화, 소규모 충분 |
| 실시간 | WebSocket (ws) | 웹 채팅 샌드박스용 |
| 검증 | Zod | Claude 출력 JSON 검증 |
| 개발 | tsx, vitest | 빠른 TS 실행, 테스트 |

### 확장 경로

```
현재 (v1): 단일 서버 + SQLite
     ↓
v2: PostgreSQL 전환 + 임베딩 기반 매칭
     ↓
v3: 멀티 인스턴스 + Redis 세션 + BullMQ 큐
     ↓
v4: MSA 분리 (매칭 서비스, 채팅 서비스, AI 서비스)
```

---

## 12. 데이터 모델

### ERD

```
users (1) ──── (1) profiles
  │
  │ (1)
  │
  ├──── (N) matches [as user_a]
  ├──── (N) matches [as user_b]
  │
  │ (1)
  └──── (1) conversation_state

matches (1) ──── (N) messages
```

### 전체 스키마

```sql
-- 사용자 계정
CREATE TABLE users (
  id         TEXT PRIMARY KEY,        -- UUID v4
  phone      TEXT NOT NULL UNIQUE,    -- E.164
  state      TEXT NOT NULL DEFAULT 'onboarding',
  created_at INTEGER NOT NULL,        -- Unix ms
  updated_at INTEGER NOT NULL
);

-- 익명 프로필
CREATE TABLE profiles (
  id           TEXT PRIMARY KEY,
  user_id      TEXT NOT NULL UNIQUE REFERENCES users(id),
  display_name TEXT,
  age_range    TEXT,
  looking_for  TEXT,                  -- 'friends'|'dating'|'both'
  interests    TEXT NOT NULL,         -- JSON 배열
  personality  TEXT NOT NULL,         -- JSON 객체
  bio_summary  TEXT NOT NULL,
  embedding    BLOB,                  -- 향후 벡터 유사도용
  completed    INTEGER NOT NULL DEFAULT 0,
  created_at   INTEGER NOT NULL,
  updated_at   INTEGER NOT NULL
);

-- 매칭 레코드
CREATE TABLE matches (
  id                  TEXT PRIMARY KEY,
  user_a_id           TEXT NOT NULL REFERENCES users(id),
  user_b_id           TEXT NOT NULL REFERENCES users(id),
  score               REAL NOT NULL,
  channel_token       TEXT NOT NULL UNIQUE,
  proxy_number_a      TEXT,
  proxy_number_b      TEXT,
  status              TEXT NOT NULL DEFAULT 'active',
  message_count       INTEGER NOT NULL DEFAULT 0,
  recommendation_sent INTEGER NOT NULL DEFAULT 0,
  a_agreed_exchange   INTEGER NOT NULL DEFAULT 0,
  b_agreed_exchange   INTEGER NOT NULL DEFAULT 0,
  created_at          INTEGER NOT NULL,
  updated_at          INTEGER NOT NULL,
  UNIQUE(user_a_id, user_b_id)
);

-- 메시지 기록
CREATE TABLE messages (
  id         TEXT PRIMARY KEY,
  match_id   TEXT NOT NULL REFERENCES matches(id),
  sender_id  TEXT NOT NULL REFERENCES users(id),
  body       TEXT NOT NULL,
  channel    TEXT NOT NULL,           -- 'sms'|'web'
  flagged    INTEGER NOT NULL DEFAULT 0,  -- 부적절 내용 플래그
  created_at INTEGER NOT NULL
);

-- 온보딩 대화 상태
CREATE TABLE conversation_state (
  id         TEXT PRIMARY KEY,
  user_id    TEXT NOT NULL UNIQUE REFERENCES users(id),
  history    TEXT NOT NULL DEFAULT '[]',  -- JSON [{role, content}]
  turn_count INTEGER NOT NULL DEFAULT 0,
  updated_at INTEGER NOT NULL
);
```

---

## 13. 화면 흐름 (UX Flow)

### 온보딩 대화 흐름

```
[사용자 → Spark]  "안녕"

[Spark → 사용자]  "안녕하세요! 저는 Spark예요 ✨
                   새로운 사람을 만날 수 있게 도와드리는 AI 매칭 도우미입니다.
                   몇 가지 이야기를 나눠보면서 잘 맞는 분을 찾아드릴게요.
                   (걱정 마세요, 이름이나 주소 같은 건 절대 안 물어봐요)
                   먼저, 어떤 관계를 찾고 계세요?
                   친구를 사귀고 싶은지, 연애 상대를 찾는 건지, 아니면 일단 만나보고 싶은지요?"

[사용자]  "친구도 좋고 연애도 좋아요"

[Spark]   "오, 열린 마음이시네요! 좋아요 😊
           그럼 평소에 어떤 걸 즐겨 하세요?
           취미나 관심사 아무거나 말씀해주세요!"

[사용자]  "독서랑 카페 가는 거 좋아해요. 요즘은 등산도 시작했어요"

[Spark]   "와, 독서에 카페 탐방, 등산까지! 활동적인 편이시네요.
           혹시 어떤 장르 책을 좋아하세요? 아니면 제일 최근에 읽은 책이 있나요?"

... (계속)

[Spark]   "이야기 들어보니 정말 재미있는 분이신 것 같아요!
           제가 파악한 프로필이에요:
           ─────────────────────
           닉네임: (없음)
           나이대: 20대 후반
           관심사: 독서, 카페 탐방, 등산, 영화
           성격: 호기심 많음, 활동적, 사려깊음
           목적: 친구 또는 연애
           ─────────────────────
           이 정도면 맞나요? 수정할 부분 있으면 말씀해주세요!
           없으면 '맞아요'라고 해주시면 매칭을 시작할게요."

[사용자]  "맞아요"

[Spark]   "완벽해요! 이제 잘 맞는 분을 찾아볼게요 🔍
           매칭이 되면 바로 문자로 알려드릴게요!"
```

### 매칭 알림

```
[Spark → 사용자 A]
"좋은 소식이에요! 🎉 잘 맞을 것 같은 분을 찾았어요.

상대방 닉네임: 별이 (20대 후반)
공통 관심사: 독서, 카페 탐방
Spark 한마디: 둘 다 독서를 좋아하고 새로운 것에 호기심이 많아서 대화가 잘 통할 것 같아요!

이 번호(+8210-9002)로 문자를 보내면 익명으로 대화가 시작돼요.
(상대방은 여러분의 실제 번호를 볼 수 없어요 🔒)"
```

### 정보 교환 추천

```
[Spark → 사용자 A]
"두 분이 정말 잘 맞는 것 같아요! ✨
대화도 자연스럽고, 독서와 카페 탐방 외에도 음악 취향도 비슷하더라고요.

서로 연락처를 교환하고 싶다면 '교환'이라고 답장해주세요.
상대방도 동의하면 서로의 번호를 알려드릴게요!
(원하지 않으면 무시하셔도 돼요. 대화는 계속됩니다)"

[사용자 A]  "교환"

[Spark → 사용자 A]
"좋아요! 상대방의 동의를 기다리고 있을게요 😊"

[상대방도 "교환" 전송 후]

[Spark → 사용자 A]
"상대방도 동의했어요! 🎊
별이의 연락처: +8210-xxxx-xxxx
이제 직접 연락하실 수 있어요. 좋은 만남 되세요! ✨"
```

---

## 14. SMS 대화 스크립트 예시

### HELP 명령어 응답

```
[Spark]
📋 Spark 사용법

• 문자를 보내면 AI와 대화가 시작돼요
• 매칭이 되면 익명으로 대화할 수 있어요

명령어:
• LEAVE — 현재 매칭 종료
• 프로필 — 내 프로필 보기
• 수정 — 프로필 수정
• STOP — 서비스 중단
• START — 서비스 재시작

도움이 필요하면 언제든 'HELP'를 보내주세요!
```

### LEAVE 명령어 응답

```
[Spark]
현재 매칭을 종료했어요.
새로운 매칭을 찾아볼게요! 잠시 기다려주세요 🔍
```

---

## 15. 운영 정책

### 이용 제한

- 1개 전화번호로 동시 1개 매칭만 활성화
- 하루 최대 3번 `LEAVE` (무한 반복 방지)
- 연속 5번 부적절 콘텐츠 신고 시 → 영구 차단

### 콘텐츠 정책

**금지 항목:**
- 음란물, 성적 표현 (온보딩/채팅 모두)
- 욕설, 혐오 표현
- 사기, 스팸
- 개인정보 공유 유도
- 미성년자 관련 부적절 내용

**감지 방법:**
- AI 실시간 모니터링
- 키워드 필터링
- 사용자 신고

### 비활성 처리

| 상태 | 기간 | 처리 |
|---|---|---|
| waiting | 7일 비활성 | 알림 문자 발송 |
| waiting | 14일 비활성 | inactive로 전환 |
| chatting | 72시간 무응답 | 매칭 상대에게 알림 |
| chatting | 7일 무응답 | 매칭 종료 |

---

## 16. 개인정보 및 보안 정책

### 수집하는 정보

| 항목 | 용도 | 보관 기간 |
|---|---|---|
| 전화번호 (E.164) | 사용자 식별, SMS 발송 | 계정 존재 기간 |
| 대화 내용 (온보딩) | 프로필 생성 | 프로필 완성 후 30일 |
| 채팅 메시지 | AI 모니터링, 안전 검토 | 매칭 종료 후 90일 |
| 프로필 정보 | 매칭 알고리즘 | 계정 존재 기간 |

### 수집하지 않는 정보
- 본명
- 이메일
- 주소 (거주지, 직장, 학교)
- 사진
- 정확한 나이 (나이대만)

### 암호화
- 전화번호: AES-256으로 DB 암호화
- API 키: 환경변수, 코드에 하드코딩 금지
- Twilio 서명 검증으로 webhook 위조 방지
- HTTPS 필수 (TLS 1.2 이상)

### 데이터 삭제
- `STOP` 명령 → 즉시 비활성화
- 삭제 요청 시 → 7일 내 모든 데이터 삭제
- 자동 삭제: 1년 이상 비활성 계정

---

## 17. KPI 및 성공 지표

### 핵심 지표

| 지표 | 설명 | 목표 (론칭 3개월) |
|---|---|---|
| 온보딩 완성률 | 첫 문자 → 프로필 완성 비율 | ≥ 60% |
| 매칭 성공률 | 온보딩 완성 → 매칭 발생 비율 | ≥ 70% |
| 채팅 지속률 | 매칭 후 20개 이상 메시지 교환 비율 | ≥ 40% |
| 정보 교환률 | AI 추천 후 실제 교환 동의 비율 | ≥ 30% |
| 재이용률 | LEAVE 후 재매칭 시도 비율 | ≥ 50% |

### 품질 지표

| 지표 | 설명 | 기준 |
|---|---|---|
| 매칭 만족도 | 채팅 지속 시간으로 간접 측정 | 평균 ≥ 15개 메시지 |
| 오탐율 | 부적절하지 않은 콘텐츠가 필터된 비율 | ≤ 5% |
| 응답 지연 | 온보딩 AI 응답 시간 | 평균 ≤ 5초 |
| Webhook 처리율 | 15초 내 200 OK 반환 비율 | ≥ 99.9% |

---

## 18. 개발 로드맵

### MVP (v1.0) — 핵심 기능

**목표:** SMS 기반 온보딩 → 매칭 → 익명 채팅 → 정보 교환 추천 전체 흐름 완성

```
Week 1: 기반 설정
  ✓ 프로젝트 초기화 (package.json, tsconfig, .env)
  ✓ DB 스키마 + 마이그레이션
  ✓ 레포지토리 레이어
  ✓ Twilio SMS 발신 서비스

Week 2: AI 핵심 기능
  ✓ Claude API 통합 (3가지 호출)
  ✓ 프롬프트 설계 + 테스트
  ✓ 온보딩 서비스

Week 3: 매칭 + 채팅
  ✓ 상태 머신 구현
  ✓ 매칭 알고리즘
  ✓ SMS 프록시 라우팅
  ✓ Twilio Webhook 엔드포인트

Week 4: 모니터링 + 마무리
  ✓ 채팅 모니터링 서비스
  ✓ 정보 교환 추천 시스템
  ✓ 특수 명령어 처리
  ✓ 에러 핸들링
  ✓ 통합 테스트
```

### v1.1 — 안정성 강화

```
- 부적절 콘텐츠 필터링 강화
- 신고 기능 구현
- 비활성 계정 자동 처리
- 모니터링 대시보드 (관리자용)
- 성능 최적화 (응답 시간 개선)
```

### v1.2 — 웹 채팅 샌드박스

```
- 웹 기반 채팅 UI (/chat/:token)
- WebSocket 실시간 통신
- SMS ↔ 웹 채팅 메시지 동기화
- 모바일 최적화 UI
```

### v2.0 — 스케일업

```
- PostgreSQL 전환
- 임베딩 기반 매칭 (벡터 유사도)
- Redis 세션 캐싱
- BullMQ 비동기 작업 큐
- 다중 언어 지원 (영어 등)
```

---

## 부록: 환경 설정

### 필요한 외부 서비스 계정

1. **Twilio 계정**
   - 전화번호 1개 구매 (수신용)
   - 가상번호 풀 구매 (프록시용, 최소 10개 권장)
   - Auth Token + Account SID 확인
   - Webhook URL 설정

2. **Anthropic 계정**
   - API Key 발급
   - 사용량 한도 설정 (비용 제어)

3. **ngrok (개발용)**
   - 로컬 서버 외부 노출
   - 무료 계정으로 충분

### 비용 추정 (월간, 사용자 1,000명 기준)

| 항목 | 단가 | 예상 사용량 | 월 비용 |
|---|---|---|---|
| Twilio SMS 수신 | $0.0075/건 | 10,000건 | $75 |
| Twilio SMS 발신 | $0.0079/건 | 15,000건 | $119 |
| Twilio 가상번호 | $1.00/월/번호 | 10개 | $10 |
| Claude API | ~$3/1M 토큰 | ~2M 토큰 | $6 |
| 서버 (VPS) | | | $20 |
| **합계** | | | **~$230/월** |

*활성 사용자 1,000명, 1인당 평균 25건 메시지 기준 추정*
